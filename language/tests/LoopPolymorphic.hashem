bebin add(a, b) {
  bede a + b;
}

bebin loop(n) {
  i = 0;
  ta (i < n) bood {
    i = add(i, 1); 
  }
  bede i;
}

bebin main() {
  add("a", "b");

  i = 0;
  ta (i < 20) bood {
    loop(1000);
    i = i + 1;
  }
  bechap(loop(1000));
}
